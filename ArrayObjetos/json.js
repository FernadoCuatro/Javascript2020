//Es un formato para el intercambio de datos y un subconjunto de objetos

//Objeto tradicional 
var miObjeto = {
 nombre : "Fernando"
}

//Lo que hace JSON.stringify es que convertir un valor o un objeto a una cadena de tipo JSON
console.log( JSON.stringify(miObjeto) );


